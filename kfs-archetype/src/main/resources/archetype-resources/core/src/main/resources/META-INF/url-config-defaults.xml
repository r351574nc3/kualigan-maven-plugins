#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<config>

    <param name="appserver.url">http://localhost:8080</param>

    <param name="application.url">${symbol_dollar}{appserver.url}/kfs-${symbol_dollar}{build.environment}</param>

    <param name="cas.url">${symbol_dollar}{appserver.url}/cas</param>

    <param name="externalizable.static.content.url">${symbol_dollar}{application.url}/static</param>


    <param name="externalizable.help.url">${symbol_dollar}{externalizable.static.content.url}/help/</param>
    <param name="externalizable.images.url">${symbol_dollar}{externalizable.static.content.url}/images/</param>
    <param name="externalizable.xml.url">${symbol_dollar}{externalizable.static.content.url}/xml/</param>

    <param name="kc.application.url">http://test.kc.kuali.org/kc-trunk</param>
</config>
